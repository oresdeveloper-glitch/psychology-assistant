# TODO
- [ ] Inspect current Wokwi/PlatformIO configuration (done: read `firmware/wokwi.toml`, `platformio.ini`).
- [x] Update `firmware/wokwi.toml` so Wokwi uses the expected `.pio/build/esp32dev/firmware.bin` path.
- [x] Build firmware once with PlatformIO to confirm expected output paths (verified `firmware.bin` exists).
- [ ] Re-run Wokwi simulation to verify the error is resolved.


